

function sum(number1, number2)
{
    return number1+number2;
}

const returnValued=sum(3,3); //if we not pass any value then our output will be NaN(not a number)
                            //  means undefined + undefined = NaN

console.log(returnValued);